# ================================
# Git Attributes untuk LeafLabeler
# ================================

# Normalisasi line endings
* text=auto

# Pastikan file teks utama selalu pakai LF
*.py    text eol=lf
*.md    text eol=lf
*.txt   text eol=lf
*.yaml  text eol=lf
*.yml   text eol=lf
*.json  text eol=lf

# Pastikan file batch/PowerShell tetap CRLF di Windows
*.bat   text eol=crlf
*.ps1   text eol=crlf

# Assets biner (tidak diubah line ending)
*.ico   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.docx  binary
*.exe   binary
*.dll   binary
*.pdb   binary

# Track file model besar dengan Git LFS
*.pt    filter=lfs diff=lfs merge=lfs -text
*.onnx  filter=lfs diff=lfs merge=lfs -text
